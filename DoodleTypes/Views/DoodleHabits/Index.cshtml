@model IEnumerable<DoodleTypes.Models.DoodleHabit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Doodling.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Doodling.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DoodleHabitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DoodleHabitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DoodleHabitID })
        </td>
    </tr>
}

</table>
